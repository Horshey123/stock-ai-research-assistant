# 股研 AI 安卓版使用说明

## 已经完成的部分

- 手机界面：股票分析、任务进度、报告阅读、历史报告、连接设置。
- Android 原生工程：`mobile/android`。
- 手机访问后端：使用 Tailscale Serve 的私有 HTTPS 地址。
- 电脑端启动入口：双击 `双击启动股票AI手机服务.bat`。
- APK 构建入口：双击 `双击生成安卓安装包.bat`。

## 第一次准备

电脑需要安装：

1. Tailscale for Windows，并登录自己的 Tailscale 账号。
2. Android Studio。第一次打开时接受默认选项，让它安装 Android SDK。

安卓手机需要安装 Tailscale，并登录与电脑相同的账号。

## 每次使用

1. 在电脑上双击 `双击启动股票AI手机服务.bat`。
2. 终端会显示一个以 `https://` 开头、以 `.ts.net` 结尾的地址。
3. 手机打开 Tailscale，确认状态为已连接。
4. 打开“股研 AI”，在“设置”中粘贴这个 HTTPS 地址，点击“测试并保存”。
5. 回到首页，输入六位 A 股代码开始分析。

电脑关机、退出 Tailscale 或关闭股票 AI 后端后，手机端将暂时无法请求新分析；电脑重新启动服务后即可继续使用。

## 生成安装包

安装并初始化 Android Studio 后，双击 `双击生成安卓安装包.bat`。成功后安装包位于：

`mobile/dist-apk/StockAI-1.0-debug.apk`

把 APK 发到自己的安卓手机并安装即可。如果手机提示“禁止安装未知应用”，在该提示中只允许本次使用的文件管理器安装未知应用。

## 更新手机界面

后续修改 `mobile/src` 中的页面代码后，再次双击 `双击生成安卓安装包.bat`，重新安装生成的 APK 即可。

